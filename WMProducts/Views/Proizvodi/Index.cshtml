@model IEnumerable<WMProducts.Models.Proizvod>

@{
    /**/

    ViewBag.Title = "Proizvodi";
}

<h2>Proizvodi</h2>

<p>
    @Html.ActionLink("Kreiraj novi", "Create")
</p>
<table id="proizvodi" class="table table-hover table-condensed">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Naziv)
            </th>
            <th>
                Kategorija
            </th>
            <th>
                Proizvođač
            </th>
            <th>
                Dobavljač
            </th>

            <th>
                @Html.DisplayNameFor(model => model.Cena)
            </th>

            <th>

            </th>

            <th>

            </th>
        </tr>
    </thead>
    @*<tbody>
            @foreach (var item in Model)
            {
                <tr>
                    <td>

                        @Html.DisplayFor(modelItem => item.Naziv)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Kategorija.Naziv)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Proizvođač.Naziv)
                    </td>
                    <td>
                        @Html.DisplayFor(modelItem => item.Dobavljač.Naziv)
                    </td>

                    <td>
                        @Html.DisplayFor(modelItem => item.Cena) RSD
                    </td>
                    <td>
                        @Html.ActionLink("Izmeni", "Edit", new { id = item.Id }) |
                        @Html.ActionLink("Informacije", "Details", new { id = item.Id }) |
                        @Html.ActionLink("Obriši", "Delete", new { id = item.Id })
                    </td>
                </tr>
            }
        </tbody>*@
</table>

<link href="//cdn.datatables.net/1.10.19/css/jquery.dataTables.min.css" rel="stylesheet" />
@section scripts {

    <script src="//cdn.datatables.net/1.10.19/js/jquery.dataTables.min.js"></script>


    <script>
        $(document).ready(function () {
            $("#proizvodi").DataTable(
                {
                    "ajax": {
                        "url": "/Proizvodi/GetData",
                        "type": "GET",
                        "dataType": "json"
                    },
                    "columns": [
                        {
                            "data": "Naziv",
                            "render": function (data, type, Proizvod) {
                                return "<a href='/Proizvodi/Details/" + Proizvod.Id + "'>" + Proizvod.Naziv + "</a>";
                            }
                        },
                        {
                            "data": "Kategorija.Naziv"
                        },
                        {
                            "data": "Proizvođač.Naziv"
                        },
                        {
                            "data": "Dobavljač.Naziv"
                        },
                        {
                            "data": "Cena"
                        },
                        {
                            "data": "Naziv",
                            "render": function (data, type, Proizvod) {
                                return "<a href='/Proizvodi/Edit/" + Proizvod.Id + "' + class='btn btn-sm btn-default' + >" + "Izmeni" + "</a>";
                            }
                        },
                        {
                            "data": "Naziv",
                            "render": function (data, type, Proizvod) {
                                return "<a href='/Proizvodi/Delete/" + Proizvod.Id + "' + class='btn btn-sm btn-danger' + >" + "Obriši" + "</a>";
                            }
                        }

                        //{
                        //    data: "Id",
                        //    render: function (data) {
                        //        return "<button class='btn btn-sm btn-warning js-delete' data-proizvod-id=" + data + ">Obriši</button>";
                        //    }
                        //}
                    ]
                });

            //$("#proizvodi").on("click", ".js-delete", function () {
            //    var button = $(this);
            //    var nazivProizvoda = $(this).closest("tr").find('td:eq(0)').text();
            //    var deleteMessge = "Da li si siguran da želiš da obrišeš " + "<b>" + nazivProizvoda + "</b> ?";


            //    bootbox.confirm(deleteMessge, function (result) {
            //        if (result) {
            //            $.ajax({
            //                type: "post",
            //                url: "/Proizvodi/Delete/" + button.attr("data-proizvod-id"),
            //                ajaxasync: true,
            //                success: function () {
            //                    table.row(button.parents("tr")).remove().draw();
            //                }
            //            });
            //        }
            //    });

            //});
        });
    </script>
}

